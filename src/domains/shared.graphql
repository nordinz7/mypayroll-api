scalar DateTime
scalar JSON
scalar UUID

type TimeStamp {
  createdAt: DateTime!
  updatedAt: DateTime!
}

type numberPagination {
  count: Int
  limit: Int
  offset: Int
}

type Response {
  success: Boolean
  message: String
  data: JSON
}

enum ActiveStatus {
  ACTIVE
  DELETED
}
