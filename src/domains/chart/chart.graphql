type Chart {
  id: Int
  quantity: Int
  product: Product
  customer: User
  productId: Int
  userUuid: UUID
  createdAt: DateTime
  updatedAt: DateTime
}

type Charts {
  rows: [Chart]
  pageInfo: numberPagination
}

input ChartsQueryInput {
  productId: Int
  userUuid: UUID
  limit: Int
  offset: Int
}

input ChartInput {
  productId: Int
  quantity: Int
  userUuid: UUID
}

type Query {
  chart(id: Int): Chart
  charts(input: ChartsQueryInput): Charts
}

type Mutation {
  addChart(input: ChartInput): Chart
  updateChart(id: Int, input: ChartInput): Chart
  deleteChart(id: Int): Response
}
