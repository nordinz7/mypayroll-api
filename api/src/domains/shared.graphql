scalar DateTime
scalar JSON

type TimeStamp {
  createdAt: DateTime!
  updatedAt: DateTime!
}

type numberPagination {
  count: Int
  limit: Int
  offset: Int
}

type Response {
  success: Boolean
  message: String
}
